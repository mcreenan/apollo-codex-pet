Repair only the `idle` six-frame horizontal animation strip for Codex pet `apollo-sticker`.

Use the attached current `idle` pose-family crop as an exact literal stance template, plus the canonical base for markings and style. The existing row failed because it changed to a seated pose while the shipped anchor is standing. Every frame must reproduce the anchor's exact standing-at-rest posture, camera, apparent scale, vertical placement, footing, legs, paws, tail, head position, and sticker silhouette. It is not a sitting pose.

Output exactly six evenly spaced full-body frames on perfectly flat #00B140 chroma green. These must be consecutive micro-in-between frames in a closed loop: chest and shoulders rise only about 2% by mid-loop then settle, ears relax slightly, and a soft blink spans two adjacent mid-loop frames. Do not move legs, paws, tail, head, footing, camera, or pose. Frame 6 must flow nearly identically into frame 1.

No text, shadows, effects, ground, guide marks, clipping, or chroma green in Apollo. Use the layout guide only for six slots and never copy it.
